'use strict';

const main = () => {
  const passStrengthChecker = require('zxcvbn');

  let strength = {
    0: 'Worst',
    1: 'Bad',
    2: 'Weak',
    3: 'Good',
    4: 'Strong'
  };
};

window.addEventListener('load', main);
